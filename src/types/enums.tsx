export enum UserType {
  ADMIN = "admin",
  USER = "user",
}
export enum PaymentStatus {
  SUCCESS = "success",
  FAIL = "fail",
}

export enum OrderStatusEnum {
  created = "CREATED",
  delivered = "DELIVERED",
  delivering = "DELIVERING",
  refund = "REFUND",
}
